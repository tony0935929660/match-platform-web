# LINE Login 設定
# 從 LINE Developers Console 取得這些值
# https://developers.line.biz/console/

# LINE Channel ID (必填)
VITE_LINE_CHANNEL_ID=your_channel_id_here

# LINE Channel Secret (必填)
# 注意：在生產環境中，建議將 token 交換流程移至後端以保護此密鑰
VITE_LINE_CHANNEL_SECRET=your_channel_secret_here

# LINE Redirect URI (選填)
# 預設為 ${window.location.origin}/auth/line/callback
# 確保此 URI 已在 LINE Developers Console 中註冊
VITE_LINE_REDIRECT_URI=http://localhost:5173/auth/line/callback
